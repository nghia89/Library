@model PagedList.IPagedList<BiTech.Library.Models.TrangThaiSachViewModels>
@using PagedList.Mvc
@{
    ViewBag.Title = "DanhSachTrangThai";
    Layout = "~/Views/Shared/_LayoutMain.cshtml";

}
<div class="Title">Danh Sách Trạng Thái</div>
<p>@Html.ActionLink("Thêm Mới", "Them", null, new { @class = "btn btn-primary btn-sm" })</p>
<table class="table table-hover table-bordered table-striped" border="1">
    <thead>
        <tr class="table-info">
            <th width="50" align="right">STT</th>
            <th width="50%">Tên tình trạng</th>
            <th width="30%">Loại tình trạng</th>
            <th></th>
        </tr>
    </thead>
    @{int i = 1;}
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td align="center" >@(i + (ViewBag.pages - 1) * ViewBag.pageSize)</td>
                <td>@item.TenTT</td>
                <td>@(item.TrangThai ? "Cho phép mượn sách" : "Không cho phép mượn sách")</td>
                <td>
                    <button class="color-white btn btn-outline-primary btn-sm" onclick="window.location.href='@Url.Action("Sua", new { id = item.Id })'">
                        <i class="fa">&#xf044;</i> Sửa
                    </button>
                </td>
            </tr>
            { i++; }
        }
    </tbody>
</table>

<div>
    @Html.PagedListPager(Model, page => Url.Action("Index", new { page }), PagedListRenderOptions.ClassicPlusFirstAndLast)
</div>



