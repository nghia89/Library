@model BiTech.Library.Models.DataFieldMarcVm
@{
                /**/

                ViewBag.Title = "ImportMarc";
                Layout = "~/Views/Shared/_LayoutMain.cshtml";
}

<div class="Title">Thêm dữ liệu từ file Marc</div>
@using (Html.BeginForm("ImportMarc", "Marc", FormMethod.Post, new { @class = "form-horizontal", role = "form", enctype = "multipart/form-data" }))
{
    <div class="form-group row">
        <div class="col-md-12">
            <button type="button" class="btn btn-primary btn-sm" onclick="window.location='@Url.Action("Index", "Sach")'">
                <i class="fa fa-arrow-circle-left" style="padding-right: 4px;font-size: 17px;"></i>Trở lại
            </button>
        </div>
    </div>

    @Html.AntiForgeryToken()
    <div class="form-group row">
        <div class="col-sm-3">
            <div class="custom-file">
                <input class="custom-file-input" accept=".mrc" data-val="true" data-val-required id="LinFileMarc" name="LinFileMarc" type="file" value="aaa">
                <span style="z-index:100" id="urlFile"></span>
                <label class="custom-file-label" for="LinkExcel">Chọn file</label>
                @Html.ValidationMessageFor(m => m.LinFileMarc)

            </div>
        </div>
    </div>
    <div class="form-group row">
        <div class="col-md-offset-2 col-md-12">
            <input type="submit" class="btn btn-sm btn-primary" value="Chọn" />
        </div>
    </div>

}

@section scripts{
    <script>

        $(function () {
            $('#LinFileMarc').on('change', function () {
                var filePath = $(this).val();
                //console.log(filePath);
                $('#urlFile').html(filePath);
            });
        });

            //function readURL(input) {
            //    if (input.files && input.files[0]) {
            //        var reader = new FileReader();
            //        reader.onload = function (e) {
            //            $('#SaveImage').attr('src', e.target.result);
            //        }
            //        reader.readAsDataURL(input.files[0]);
            //    }
            //}
            //$(document).ready(function () {
            //    readURL($('#HinhChanDung'));
            //});
    </script>
}








